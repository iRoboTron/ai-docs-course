# История изменений

Формат записи: дата, модуль, что изменилось, как проверено. Сверху — новые.

## 2026-09-19 — Модуль 7: каркас сервиса (урок 1)

**Что сделано**

- Новая тема `07-servis`: `book.md`, «Урок 1. FastAPI поверх RAG из модуля 6»,
  словарик (Сервис, Тест без сети, CI). Зарегистрирована в `files.json`,
  `index.html` (COURSE_META), `reader.html` (COURSE_NAMES/COURSE_ALIASES),
  версия ассетов поднята до `20260919-modul7`.
- README: ссылка на репозиторий сервиса в начале части 2.
- Личные ноутбуки (`notebooks-extra/`) получили факультативные ссылки из заданий
  модулей 2 и 6 — необязательное чтение, вне программы курса.
- Компаньон-репозиторий `ai-docs-course-bot` (код готов локально, отдельный коммит):
  `app/rag.py` — дословный перенос `notebooks/modul6__01_citaty_i_kachestvo.py`
  в функции сервиса без изменения логики; `app/main.py` — `GET /health`,
  `POST /ask`; тесты через подмену зависимостей (`indeks`, `ask`), не ходят
  в сеть; workflow CI на GitHub Actions.

**Проверено**

- `AI_KEY=test-key PYTHONPATH=. pytest -q` в `ai-docs-course-bot`: 10 passed.
- `scripts/build_notebooks.py --check` и `scripts/check_project.py` — PASS,
  тем 7, уроков 13, схем 51.

**Решение**

- Хранилище (файл / SQLite / pgvector) вынесено в следующий урок: в этом —
  только то, что от него ожидает `otvetit()` (`indeks.nayti(vopros)` →
  список кусков), а не как куски лежат на диске.
- Публикация `ai-docs-course-bot` на GitHub — отдельное решение с владельцем
  курса (публичность репозитория), не принято этой сессией автоматически.

## 2026-09-19 — Модуль 6: полноценные фреймворки для оценки

**Что сделано**

- Урок 2 «Как мерить ответы», новый раздел «Готовые фреймворки для оценки»:
  термин-блок и таблица (RAGAS, DeepEval, promptfoo, LangSmith) с привязкой
  к тому, что курс уже делает руками (эталонный набор, судья, `pytest`,
  `decisions.md`, `cases.jsonl`).
- Урок 2: абзац в «Что показала лабораторная» с числами RAGAS по тому же
  набору — правдивость 0.96, полнота поиска 0.86; единственный провал
  («гарантия») совпал с провалом, который находит наш код.
- Ноутбук модуля 6: новый шаг 9 «Готовый фреймворк оценки: RAGAS» — две
  метрики (`faithfulness`, `context_recall`) через `llm_factory` на том же
  прокси. Шаг «Задания» стал шагом 10.
- Глоссарий: термин «Фреймворк для оценки (evaluation framework)» со схемой.
- Версия ассетов поднята до `20260919-modul6`.

**Проверено**

- Прогон RAGAS на живой модели (`qwen3.7-flash`, прокси ai9): числа совпали
  в двух прогонах.
- `scripts/build_notebooks.py --check` — PASS.
- `scripts/check_project.py` — PASS, схем стало 47.

**Решение**

- RAGAS взят как профильный фреймворк для оценки RAG; из метрик выбраны
  только те, что работают без внешних эмбеддингов через учебный прокси.
- Вопрос «оплатить картой?» (ответа нет) исключён из прогона: метрики RAGAS
  для отказа не задуманы, отказы остаются нашей проверкой.