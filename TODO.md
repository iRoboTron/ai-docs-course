# TODO: темы для новых уроков

## Решено (2026-09-22): библиотеки — часть основной программы с модуля 8

Instructor и Guardrails AI в модулях 2 и 6 были факультативными дублями
(`notebooks-extra/`, вне программы): сначала своими руками, потом «то же самое
готовой библиотекой» — необязательное чтение.

**С модуля 8 формат меняется** — по прямому запросу пользователя приблизить курс
к реальной практике. Библиотека встраивается в основной урок как обязательная
часть, а не факультатив. Принцип решения тот же, что и раньше: сначала своими
руками (курс уже так учит), библиотека — когда она закрывает конкретную проблему
модуля, а не «для галочки». Список закреплён по модулям:

| Модуль | Проблема модуля | Библиотека |
|---|---|---|
| 8. Диалог на сайте | Память и сжатие контекста диалога | **LangChain** |
| 9. В работе | Резервная модель, стоимость за вызов, лимиты | **LiteLLM** — роутинг, fallback, подсчёт цены |
| 10. Безопасность | Подмена инструкций, персональные данные | **Guardrails AI** — готовые проверки `detect_jailbreak`, `detect_pii` |
| 12. Документы клиента → факты | Структурированные факты из текста (стороны, даты, суммы) | **Instructor** |

LiteLLM в курсе ещё не трогали — при написании модуля 9 нужен тот же цикл
проверки, что был с Instructor/Guardrails: поставить в venv, свежий API через
`context7`, реально прогнать код, а не полагаться на память.

## Инструменты для работы с LLM: Instructor, Guardrails AI, LangChain

Три библиотеки решают разные задачи и не заменяют, а дополняют друг друга.

### Instructor — структурированный вывод

- Превращает ответы LLM в валидированные Pydantic-модели.
- Автоматически повторяет запрос при ошибке формата.
- Это замена ручного цикла «вызов → ValidationError → повтор с фидбеком».

### Guardrails AI — валидация и безопасность выходов

- Проверяет ответы на соответствие правилам.
- Умеет выбирать стратегию при ошибке: REASK, FIX, FILTER, EXCEPTION.
- Ведёт журнал вызовов, но не управляет контекстом диалога.

### LangChain — память и сжатие контекста

- Единственная из трёх с полноценной системой памяти:
  - буфер истории;
  - скользящее окно последних N сообщений;
  - суммаризация старых сообщений;
  - гибридные схемы.
- Трёхуровневое сжатие контекста: offloading больших данных + суммаризация.
- Долговременная память через LangMem / LangGraph.

### Важные оговорки

- Instructor и Guardrails — stateless: каждый вызов начинается «с чистого листа».
- LangChain — stateful.
- В реальных проектах их комбинируют:
  - Instructor — для структуры ответа;
  - Guardrails — для безопасности;
  - LangChain — для памяти и сжатия контекста.

### Опционально упомянуть

- `json-repair` — локальная починка сломанного JSON без повторного вызова модели.
- Режим Structured Outputs (`strict: true`) у OpenAI — «первый рубеж» защиты
  от ошибок формата.